let students = ["hodo", "mawlid", "yonis", "alin"]
students[1] = "rahma"
//students.push("huda")
// students.pop()
// students.pop()
//students.unshift("mohamed")
// students.shift()
// console.log(students)
//let numbers = [20, 3, 40, 50, 8]
//console.log(numbers[0] + numbers[numbers.length -1])
//console.log(numbers.length)
let colours = ["blue, "red", "green", "white","yallow"]
let themaincolours = colours.splice(0, 2)
consol.log(themaincolours)